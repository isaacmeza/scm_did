To replicate the figure for the SCM effect of sugary drinks you need to run (and modify the directory paths in the first line of code) the following files:

1) global_panel_cleaning.do

2) SCM_synth.R


Note that all datasets need to be allocated in a folder named 'DB'
